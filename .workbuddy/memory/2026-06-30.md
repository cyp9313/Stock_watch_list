# 2026-06-30

## 修复 KDJ 算法
- 文件：`stock_watch_list_back_end.py`，`get_kline_data` 路由
- 问题：`ewm(com=3)` 对应 alpha=0.25，与标准 KDJ 要求的 alpha=1/3 不符；rolling 窗口用的 14 日而非标准 9 日；RSV 分母未做除零保护
- 修复：改为 `rolling(9)` + 分母替换 0 为 NaN + `fillna(50)` + `ewm(com=2, adjust=False)`

## 斐波那契功能改进
- 文件：`Market_Watchlist_front_end_local.py`，`FibonacciSelector` 类
- 新增：`c` 键触发斐波那契 Extension（a/b 选回撤高低点后，按 c 再点图，自动清除回撤线并画 Extension 线）
- Extension 0% 线在 C 点位置，公式 `Level = C + (B - A) * ratio`（与 TradingView 一致）
- 比例：`0% / 23.6% / 38.2% / 50% / 61.8% / 78.6% / 100% / 127.2% / 161.8% / 200% / 261.8%`，>100% 蓝色，≤100% 灰色
- 修复：重画前清除旧线（`clear_fib_lines` 方法）；按 `a` 重选低点时也自动清除旧线
- 标注格式：`价格 (比例)`，带白色半透明背景框

## 新增神奇九转 (TD Sequential)
- 文件：`Market_Watchlist_front_end_local.py`
- 新增 `calculate_td_sequential` 函数：买入结构（连续收盘 < 4日前收盘）计1-9，卖出结构（连续收盘 > 4日前收盘）计1-9
- GridSpec 从 5 行改为 6 行，figsize 从 (12,12) 改为 (12,14)
- 新增 ax6 子图（RSI 下方）：红色柱=卖出序列（潜在顶部），绿色柱=买入序列（潜在底部），9 加粗
- 主图 ax1 同步标注：卖出数字在 K 线上方红色，买入数字在 K 线下方绿色

## 修复 zoom in 后文字溢出坐标轴
- 文件：`Market_Watchlist_front_end_local.py`
- 问题：matplotlib `ax.text()` 默认 `clip_on=False`，zoom in 后超出可见区域的文字仍会渲染到图外
- 修复：所有 `ax.text()` 调用加 `clip_on=True`（TD Sequential 4 处 + 斐波那契回撤/扩展各 1 处，共 6 处）

