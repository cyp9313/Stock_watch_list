# 2026-07-01

## K线图坐标显示改为工具栏 format_coord
- 文件：`Market_Watchlist_front_end_local.py`
- 问题：motion_notify_event + annotate + draw_idle 导致鼠标移动卡顿
- 修复：去掉浮动注释框，改为重写 `ax1.format_coord` — 工具栏原生显示 O/H/L/C/Vol 文字，零画布重绘
- 用 `np.searchsorted` 二分查找最近 K 线，O(log n)

## 新增 Streamlit 版本
- 文件：`streamlit_app.py`
- 基于 Streamlit + Plotly 完整重写美股监控面板
- 功能：
  - 侧边栏分组选择器 + 刷新按钮
  - CNN/Crypto 恐惧贪婪指数彩色显示
  - 数据表格（24列），带红绿涨跌色、蓝色量比/PE/市值色、财报日期色
  - Plotly K线图：OHLC + MA5-200 + 布林带 + 成交量 + MACD + KDJ + RSI + TD Sequential + 筹码分布
  - 斐波那契回撤/扩展（输入 A/B/C 价格点）
  - 市场宽度折线图（MA20/50/200 + 15%/85% 参考线）
  - 所有 API 调用带 @st.cache_data 缓存
- 启动方式：`streamlit run streamlit_app.py`

## 修复 Streamlit 版 groups 参数格式错误
- 问题：fetch_stock_data 发送 `groups=Mag7,Chips/AI`（逗号分隔组名），后端期望 JSON 格式的完整分组字典
- 修复：`fetch_stock_data` 改为 `json.dumps({g: GROUPS[g] for g in selected_groups})`，并添加 `import json`
